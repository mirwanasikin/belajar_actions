name: Learn Github Actions

on:
  workflow_dispatch:

jobs:
  learn_github:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo Hello World
